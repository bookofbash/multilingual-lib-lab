# Prompt-ops

Cross-project prompt playbook and token ledger live in `~/Documents/prompt-ops`, not in this repo.

- **Eval recipes stay here** (how-tos, challenge set, `PHENOMENA.md`).
- **Index + spend** go there. After a substantial agent job, log one ledger event (`log-session` skill). Project id: `multilingual-lib-lab`.

Do not copy session transcripts, journals, or secrets into the ledger.
